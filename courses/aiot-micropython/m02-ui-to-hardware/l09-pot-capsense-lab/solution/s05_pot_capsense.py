# s05_pot_capsense.py - ลูกบิด + แถบสัมผัส + ฟิลเตอร์ EMA
# วิธีรัน: 1) บนจอบอร์ด แตะการ์ด Playground ค้างไว้ตลอดบทเรียน
#          2) กด Program to Device แล้วหมุนลูกบิดช้า ๆ ดูแถบกับไม้บรรทัดขยับพร้อมกัน
#          3) กดปุ่มเพิ่ม/ลดเกณฑ์ แล้วหมุนลูกบิดข้ามเกณฑ์ ดูไฟสองดวงสลับกันติด
#
# หนึ่งค่าที่วัดได้ ถูกเล่าสามภาษาพร้อมกัน: ค่าดิบสำหรับดูความละเอียด
# เปอร์เซ็นต์สำหรับคนทั่วไป และโวลต์สำหรับให้เห็นว่าค่าดิบแปลงกลับเป็นไฟฟ้าได้
#
# ดูที่จอ: การ์ดบนซ้ายคือค่าลูกบิดที่วางอยู่บนไม้บรรทัดของมันเอง การ์ดบนขวาคือเกณฑ์
#         ที่ผู้ใช้ตั้งเองพร้อมไฟสองดวง การ์ดล่างซ้ายคือแถบสัมผัส และการ์ดล่างขวา
#         คือค่าดิบเทียบค่าที่กรองแล้ว
# กับดัก : ui.Scale ไม่มีเข็มและไม่รับ .value() มันคือไม้บรรทัด ตัวที่ขยับคือ ui.Bar
#         ที่เราวางทับลงไปเอง ส่วน ui.Led สั่ง .value(0) แล้ว "หรี่" ไม่ใช่ "หาย"
#         โดยตั้งใจ เพราะไฟแผงควบคุมที่หายไปตอนดับ ทำให้คนดูแยกไม่ออกว่าดับหรือจอเสีย
#         และสีของ "แถบค่า" ใน ui.Bar มาจากธีม ส่วน .color() ไปลงที่รางด้านหลัง
#         จะบอกสถานะด้วยการเปลี่ยนสีแถบจึงทำไม่ได้ ต้องบอกด้วยไฟกับตัวหนังสือ
#         ซึ่งเป็นวิธีที่ผ่านการทดสอบขาวดำอยู่แล้ว

import ui
ui.screen()          # ล้าง widget เดิมทั้งหมด เริ่มจากจอว่างที่รู้แน่
import time
import sensors
import dsp

ui.clear()
time.sleep_ms(200)

# จานสีของแผงควบคุม ไม่ใช่จานสีของงานตกแต่ง - สีจัดสงวนไว้ให้สถานะผิดปกติเท่านั้น
# ค่าปกติจึงเป็นตัวหนังสือสีขาวบนการ์ดสีเข้ม ไม่มีสีไหนแย่งสายตาไปจากไฟสองดวง
COL_TEXT, COL_DIM = 0xE8EAED, 0x9AA3AF
COL_CARD = 0x171B22
COL_OK, COL_WARN, COL_BAD = 0x30A46C, 0xF5A623, 0xE5484D

# เกณฑ์เตือนเป็นค่าที่ผู้ใช้ตั้งเอง ไม่ใช่ค่าคงที่ที่ฝังอยู่ในโค้ด
# สามบรรทัดนี้คือพิสัยที่ยอมให้ตั้ง กับก้าวของปุ่มเพิ่ม/ลด
TH_MIN, TH_MAX, TH_STEP = 10, 95, 5
th = 70              # เกณฑ์ตั้งต้น หน่วยเปอร์เซ็นต์

# --- ท่าที่ 1: ไม่ต้องปลุกเซนเซอร์ ทั้งสองบอร์ด ---
# sensors.pot กับ sensors.capsense อ่านได้เลยโดยไม่ต้อง init() ทั้ง Eva Kit และ Dev Kit
# บน Eva Kit บัส SCB0 เป็นของคอร์จอ (CM55) คนเดียว ลูกบิด แถบสัมผัส และ IMU
# อยู่บนบัสนั้นทั้งหมด ฝั่ง Python จึงไม่ได้อ่านชิปเอง แต่ขอค่าที่คอร์จอ
# อ่านเก็บไว้ให้อยู่แล้ว เพราะฉะนั้นเรียก sensors.pot.read() ได้เลย
#
# sensors.init() และ sensors.scan() บน Eva Kit จะโยน OSError ทันที ไม่ใช่ค้าง
# ใครเผลอเรียกจะเห็น error แล้วสคริปต์หยุด บอร์ดไม่ตาย ไม่ต้องถอดไฟ
# (บน Dev Kit สองชื่อนั้นผ่าน แต่ไฟล์นี้ก็ไม่จำเป็นต้องเรียกอยู่ดี)
#
# ข้อควรรู้ข้อเดียวของเส้นทางนี้: หลังรีเซ็ต การอ่านครั้งแรกอาจต้องรอคอร์จอตอบ
# อยู่พักใหญ่ ครั้งถัดไปเร็วปกติ
# เรียกอุ่นเครื่องหนึ่งครั้งตรงนี้ ให้การรอไปเกิดก่อนสร้างจอ ไม่ใช่กลางลูป
try:
    sensors.pot.read()
except OSError:
    print("อ่านเซนเซอร์รอบแรกยังไม่ได้ - ลองใหม่ในลูป")

ui.Label("แผงคุมลูกบิดกับแถบสัมผัส", x=16, y=8, color=COL_TEXT, value=24)

# --- ท่าที่ 2: ค่าที่วัดได้ ต้องมาพร้อมพิสัยของมันเอง ---
# ui.Panel คือพื้นหลังของการ์ด - min คือสีขอบ max คือความมน value คือความหนาขอบ
ui.Panel(x=12, y=40, w=468, h=200, color=COL_CARD, min=COL_DIM, max=12, value=1)
ui.Label("ลูกบิดเทียบพิสัย 0-100", x=28, y=48, color=COL_DIM, value=20)
# แถบค่าวางไว้ "เหนือ" ไม้บรรทัด เพราะ ui.Scale ไม่มีเข็มให้ - ตัวที่ขยับคือ ui.Bar
pot_bar = ui.Bar(x=28, y=80, w=428, h=16, color=0x4A9EFF, min=0, max=100, value=0)
pot_scale = ui.Scale(x=28, y=92, w=428, h=44, color=COL_TEXT, min=0, max=100)
# ไม้บรรทัดแคบ ๆ ที่มีขีดเยอะเกินไป ตัวเลขจะเบียดกันจนอ่านไม่ออก
# ขีดทั้งหมด 11 ขีด ใส่ตัวเลขทุกขีดที่สอง = 0 20 40 60 80 100 พอดี
pot_scale.ticks(11, 2)

ui.Label("เปอร์เซ็นต์ที่วัดได้", x=28, y=152, color=COL_DIM, value=20)
lbl_pct = ui.Label("0.0 %", x=28, y=180, color=COL_TEXT, value=28)
lbl_raw = ui.Label("ค่าดิบ 0", x=252, y=148, color=COL_DIM, value=16)
lbl_volt = ui.Label("โวลต์ 0.000 V", x=252, y=172, color=COL_DIM, value=16)

# ท่าเดียวกันครึ่งหลัง: เกณฑ์ที่ผู้ใช้ตั้งเอง กับไฟสองดวงที่บอกสถานะ
# ค่าที่วัดได้ต้องมีเกณฑ์อยู่ข้าง ๆ ไม่งั้นคนดูตอบไม่ได้ว่า 55 เปอร์เซ็นต์คือมากหรือน้อย
ui.Panel(x=492, y=40, w=288, h=224, color=COL_CARD, min=COL_DIM, max=12, value=1)
ui.Label("เกณฑ์เตือน (เปอร์เซ็นต์)", x=508, y=48, color=COL_DIM, value=20)
# ui.Spinbox หนีบค่าให้อยู่ในพิสัยที่ตั้งไว้เอง คนป้อนจึงพิมพ์เกินพิสัยไม่ได้เลย
# ข้อควรรู้ของจอสัมผัส: spinbox เปล่า ๆ นิ้วเปลี่ยนค่าไม่ได้ เพราะการแตะแค่เลือก
# ตำแหน่งหลัก ตัวที่เพิ่มลดค่าจริงคือปุ่มสองปุ่มข้าง ๆ นี้
sp_th = ui.Spinbox(x=508, y=76, w=256, h=88, color=COL_TEXT,
                   min=TH_MIN, max=TH_MAX, value=th)
# ค่าตั้งต้นของช่องคือสี่หลักไม่มีจุดทศนิยม เกณฑ์ของเราเป็นเลขสองหลัก
# ถ้าไม่บอก จะเห็น 0070 ซึ่งอ่านแล้วสะดุด - digits(หลักทั้งหมด, หลักหน้าจุด)
sp_th.digits(2, 0)
btn_up = ui.Button("เพิ่ม", x=644, y=172, w=120, h=88, color=0x3A4150, value=16)
btn_dn = ui.Button("ลด", x=508, y=172, w=120, h=88, color=0x3A4150, value=16)

ui.Label("สถานะเทียบเกณฑ์", x=492, y=280, color=COL_DIM, value=16)
led_ok = ui.Led(x=492, y=304, w=48, h=48, color=COL_OK, value=1)
ui.Label("ต่ำกว่าเกณฑ์", x=548, y=320, color=COL_DIM, value=16)
led_bad = ui.Led(x=492, y=348, w=48, h=48, color=COL_BAD, value=0)
ui.Label("เกิน", x=548, y=364, color=COL_DIM, value=16)

# --- ท่าที่ 3: แถบเลื่อนสัมผัสและปุ่มทองแดงสองปุ่ม ---
# ui.Bar ไม่รับการแตะและไม่ส่ง event ใด ๆ มันเป็นจอแสดงผลอย่างเดียว
# ซึ่งตรงกับที่เราต้องการพอดี เพราะตัวสั่งงานคือนิ้วบนแถบทองแดง ไม่ใช่นิ้วบนกระจก
ui.Panel(x=12, y=248, w=468, h=140, color=COL_CARD, min=COL_DIM, max=12, value=1)
ui.Label("แถบสัมผัสและปุ่มทองแดง", x=28, y=256, color=COL_DIM, value=20)
touch_bar = ui.Bar(x=28, y=284, w=428, h=16, color=0x4A9EFF, min=0, max=100, value=0)
touch_scale = ui.Scale(x=28, y=300, w=428, h=40, color=COL_TEXT, min=0, max=100)
touch_scale.ticks(11, 2)
# ไฟแทนตัวหนังสือสีสำหรับปุ่มสองปุ่ม: ถ่ายจอเป็นขาวดำแล้วยังแยกออกว่าดวงไหนติด
# ส่วนคำว่า ON สีม่วงกับ - สีเทา พอเป็นขาวดำแล้วแยกไม่ออก
led_b0 = ui.Led(x=28, y=344, w=48, h=48, color=COL_OK, value=0)
ui.Label("ปุ่ม 0", x=84, y=356, color=COL_DIM, value=20)
led_b1 = ui.Led(x=160, y=344, w=48, h=48, color=COL_OK, value=0)
ui.Label("ปุ่ม 1", x=216, y=356, color=COL_DIM, value=20)

# --- ท่าที่ 4: บรรทัดคุณภาพของค่า และการเทียบค่าดิบกับค่ากรองแล้ว ---
# ค่าที่แสดงต้องบอกคุณภาพของตัวเองได้ ค่าที่ค้างอยู่ต้องเขียนให้ชัดว่ามันค้าง
# ไม่ใช่ปล่อยเลขเดิมค้างไว้เฉย ๆ แล้วให้คนเดินมาดูเข้าใจว่ามันคือค่าปัจจุบัน
ui.Panel(x=280, y=336, w=196, h=52, color=COL_CARD, min=COL_DIM, max=12, value=1)
ui.Label("คุณภาพของค่า", x=292, y=340, color=COL_DIM, value=16)
lbl_health = ui.Label("อ่านค่าปกติ", x=292, y=364, color=COL_TEXT, value=16)
health = "อ่านค่าปกติ"      # ข้อความล่าสุดของบรรทัดคุณภาพ คิดใหม่วินาทีละครั้ง

# แสดงคู่กันบนจอเดียว ถ้าดูทีละอันจะไม่มีวันรู้ว่าฟิลเตอร์ช่วยจริงหรือแค่รู้สึกไปเอง
raw_line = ui.Label("ดิบ 0.00 %", x=252, y=196, color=COL_TEXT, value=16)
ema_line = ui.Label("กรอง 0.00 %", x=368, y=196, color=COL_TEXT, value=16)
ui.Label("อัลฟา 0.2", x=252, y=216, color=COL_DIM, value=16)
ui.poll()

# สร้างนอกลูป เพราะฟิลเตอร์ต้องจำค่าเดิมข้ามรอบ ถ้าเผลอสร้างในลูป
# ทุกรอบจะได้ตัวใหม่ที่ความจำว่างเปล่า ค่าที่ออกมาจะเท่ากับค่าดิบเป๊ะ ๆ
# แล้วเราจะสรุปผิดว่า "ฟิลเตอร์ไม่ทำงาน" - alpha เป็น keyword-only ห้ามเขียน dsp.EMA(0.2)
#
# ทำไมเป็น 0.2 ไม่ใช่ 0.5: m02-ui-to-hardware/l08-filters/examples/06_ema_time_constant.py แปลง alpha ให้เป็นเวลาจริง
# ด้วย tau = dt * (1-alpha)/alpha ที่คาบ 200 ms ของไฟล์นี้ alpha 0.2 คือ tau ราว 0.8 วินาที
# ซึ่งเป็นความหน่วงที่มือคนยังรู้สึกว่าเข็มตามทัน เลือกจากตัวเลขนั้น ไม่ใช่จากความรู้สึก
# และถ้าสัญญาณของทีมมีค่าหลุดโด่ง ๆ เป็นครั้งคราว m02-ui-to-hardware/l09-pot-capsense-lab/examples/07_median_beats_mean.py
# แสดงว่าทำไมงานแบบนั้น median ชนะค่าเฉลี่ยขาดลอย - ค่าที่หลุดไปนอนริมแถว ไม่มีสิทธิ์ถูกหยิบ
ema = dsp.EMA(alpha=0.2)

raw, pct, volts = 0, 0.0, 0.0
ema_pct = 0.0
slider = 0
last_sec = -1        # วินาทีที่เพิ่งเขียนตัวเลขลงจอ กันไม่ให้เขียนถี่กว่าวินาทีละครั้ง

# --- ท่าที่ 5: ลูปหลักและจังหวะที่ปลอดภัย ---
while True:
    fresh = True
    try:
        # สามบรรทัดนี้คือการถามคอร์จอสามรอบ ไม่ใช่รอบเดียว ถ้าอยากได้ทุกค่า
        # ในจังหวะเดียวกันเป๊ะ ให้ใช้ sensors.snapshot() ซึ่งได้ทั้งชุดในการถามครั้งเดียว
        raw = sensors.pot.read()          # 0 - 65535   จำนวนเต็ม ค่าดิบจาก ADC
        # โวลต์ทั้งสองบอร์ดไม่ได้วัดมาตรง ๆ แต่คำนวณจากเปอร์เซ็นต์คูณแรงดันอ้างอิง 3.3 V
        # และค่าอ้างอิงตัวนั้นยังไม่มีใครเอามิเตอร์ไปจิ้มขาลูกบิดยืนยันบนบอร์ดไหนเลย
        # (Eva Kit ต่อที่ P15[1] ส่วน Dev Kit คือ VR1 บนฐาน QWA309) จึงใช้ดูแนวโน้มได้
        # แต่อย่าเพิ่งเอาไปเถียงกับมัลติมิเตอร์
        # เหตุผลที่ต้องระวังคำว่า "โวลต์" ขนาดนี้ m02-ui-to-hardware/l09-pot-capsense-lab/examples/05_adc_counts_to_volts.py อธิบายไว้ครบ
        # เลขจาก ADC ไม่ใช่แรงดัน มันคือ "จำนวนขั้น" การจะเรียกว่าโวลต์ได้ต้องรู้แรงดันเต็มสเกล
        # กับจำนวนบิตก่อนเสมอ ที่นี่เรารู้ไม่ครบ บรรทัดนี้จึงเป็นค่าประมาณ ไม่ใช่ค่าวัด
        volts = sensors.pot.voltage()     # 0.0 - 3.3   คำนวณจากเปอร์เซ็นต์ ไม่ใช่ค่าวัด
        pct = sensors.pot.percent()       # 0.0 - 100.0 แปลงเป็นเปอร์เซ็นต์ให้แล้ว
        # ไฟล์นี้เอาค่าจาก slider() ขึ้นจอตรง ๆ ได้ เพราะจอไม่ได้ไปสั่งอะไรต่อ
        # แต่วันที่เอาค่านี้ไปสั่งของจริง จะต้องรู้เพิ่มอีกเรื่อง - m02-ui-to-hardware/l07-adc-capsense/examples/01_capsense_dimmer.py
        # แสดงว่าค่าสุดท้ายจะค้างอยู่ตอนปล่อยนิ้ว ไม่ได้กลับเป็นศูนย์ ต้องดูว่าค่าหยุดเปลี่ยนกี่รอบแล้ว
        # จึงจะสรุปว่านิ้วออก และ m02-ui-to-hardware/l09-pot-capsense-lab/examples/02_capsense_menu_wet_hand.py ต่อยอดเป็นเมนูที่ตอบกลับ
        # ด้วยจอ ไฟ และเสียง เพราะแผ่นสัมผัสไม่มีแรงต้านให้นิ้วรู้สึกเหมือนปุ่มกล
        slider = sensors.capsense.slider()    # 0 - 100 อยู่แล้ว ไม่ต้องแปลงหน่วย
        b0, b1 = sensors.capsense.buttons()   # คืน tuple สองช่อง แกะพร้อมกันได้เลย
    except OSError:
        # อ่านพลาดหนึ่งรอบไม่ควรทำให้จอดับทั้งหน้า ใช้ค่าเดิมไปก่อน
        # แต่ต้องบอกให้คนดูรู้ว่าตัวเลขบนจอตอนนี้ไม่ใช่ค่าปัจจุบันแล้ว
        fresh = False
        b0, b1 = 0, 0

    # ครอบขอบเขตก่อนส่งเข้า widget: ถ้าไดรเวอร์คืนค่าหลุดช่วงมา จอยังแสดงผลได้
    # แทนที่จะพังทั้งหน้า - ค่าที่มาจากภายนอกโปรแกรมเราควรถูกตรวจขอบเขตก่อนใช้เสมอ
    pot_bar.value(int(max(0, min(100, pct))))
    touch_bar.value(max(0, min(100, slider)))

    ema_pct = ema.update(pct)         # ป้อนค่าใหม่ ได้ค่าที่กรองแล้วกลับมาทันที

    # แถบกับไฟขยับได้ทุกรอบ เพราะตาคนอ่าน "ตำแหน่ง" ได้โดยไม่ต้องหยุดอ่าน
    # หนึ่งดวงติดเท่านั้น แผงที่ติดพร้อมกันหลายดวงคือแผงที่อ่านไม่ออก
    over = ema_pct >= th
    led_ok.value(0 if over else 1)
    led_bad.value(1 if over else 0)
    led_b0.value(1 if b0 else 0)
    led_b1.value(1 if b1 else 0)

    # --- ปุ่มเพิ่ม/ลดเกณฑ์: คนละปุ่มกันคนละทิศ ไม่ใช่ปุ่มเดียวสลับ ---
    for ev in ui.poll():
        if ev.get("type") != "clicked":
            continue
        if ev.get("handle") == btn_up.id():
            th = min(TH_MAX, th + TH_STEP)
            sp_th.value(th)
        elif ev.get("handle") == btn_dn.id():
            th = max(TH_MIN, th - TH_STEP)
            sp_th.value(th)

    # ตัวเลขที่คนต้องอ่าน เขียนใหม่ไม่เกินวินาทีละครั้ง และอยู่ตำแหน่งเดิมเสมอ
    # ตัวเลขที่วิ่งห้าครั้งต่อวินาทีคือตัวเลขที่อ่านไม่ทัน และคนจะเลิกอ่านมันไปเลย
    sec = time.ticks_ms() // 1000
    if sec != last_sec:
        last_sec = sec
        lbl_pct.text("{:.1f} %".format(pct))
        lbl_raw.text("ค่าดิบ {}".format(raw))
        lbl_volt.text("โวลต์ {:.3f} V".format(volts))
        # พิมพ์ทศนิยมสองตำแหน่งทั้งคู่โดยตั้งใจ ถ้าปัดเหลือจำนวนเต็ม
        # ความสั่นจะถูกซ่อนไปเอง แล้วบทเรียนทั้งบทเรียนก็หายไปด้วย
        raw_line.text("ดิบ    {:.2f} %".format(pct))
        ema_line.text("กรอง  {:.2f} %".format(ema_pct))
        # ตั้งสีก่อนแล้วค่อยเขียนข้อความ สองบรรทัดนี้เป็นคำสั่งคนละครั้งข้ามคอร์
        # ถ้าเขียนข้อความก่อน จะมีเสี้ยววินาทีที่ข้อความใหม่ยังใส่สีของสถานะเก่าอยู่
        if not fresh:
            lbl_health.color(COL_WARN)
            health = "ค่าค้าง - เลขคือค่าล่าสุด"
        elif over:
            lbl_health.color(COL_BAD)
            health = "เกินเกณฑ์ {} %".format(th)
        else:
            lbl_health.color(COL_DIM)
            health = "ปกติ ต่ำกว่า {} %".format(th)

    # --- ชีพจรของช่องทางข้ามคอร์: ส่งบรรทัดสถานะซ้ำทุกรอบโดยตั้งใจ ---
    # ฝั่งจอมีตัวจับเวลาสองจังหวะ โหมดเร็ว 5 ms กับโหมดปกติ 200 ms และมันจะอยู่
    # โหมดเร็วต่อไปอีก 500 ms ทุกครั้งที่ได้รับคำสั่ง "เขียนข้อความ" หรือ "ย้าย/ย่อ/เปลี่ยนสี"
    # แต่ .value() ของแถบกับไฟ "ไม่" ปลุกโหมดนั้น ลูปที่อัปเดตเฉพาะแถบกับไฟจึงเงียบเกิน
    # ครึ่งวินาที แล้วจอจะถอยไปโหมดช้า ภาพกระตุกทั้งที่โค้ดไม่ได้เปลี่ยนสักบรรทัด
    # ข้อความที่ส่งไปนี้เท่าเดิมเกือบทุกรอบ และเฟิร์มแวร์ไม่วาดซ้ำถ้าข้อความไม่เปลี่ยน
    # เราจึงจ่ายแค่ค่าส่งข้ามคอร์ ไม่ได้จ่ายค่าวาดใหม่
    lbl_health.text(health)

    # ทำไม 200 ms ไม่ใช่ 20 ms - ลูปที่เร็วเกินจะยิงคำสั่งวาดข้ามคอร์ถี่กว่าที่ CM55 วาดทัน
    # เฟรมส่วนเกินถูกทิ้งเงียบ ๆ ไม่มี error ให้เห็น ผลคือจอกระตุกทั้งที่โค้ดดูถูกทุกบรรทัด
    time.sleep_ms(200)
