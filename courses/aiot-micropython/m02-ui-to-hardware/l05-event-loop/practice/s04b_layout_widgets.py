# s04b_layout_widgets.py - widget ที่ "ใส่ของอื่นไว้ข้างใน" ได้ (ฉบับฝึกเติมโค้ด)
# วิธีรัน: 1) บนจอบอร์ด แตะการ์ด Playground ค้างไว้ (widget เกิดบนหน้านี้เท่านั้น)
#          2) เติมช่องว่างตามคำใบ้ให้ครบทีละท่า ส่งขึ้นบอร์ดดูผลทุกครั้ง
#          3) กด Program to Device แล้วแตะแท็บบนจอสลับไปมา
#
# บทเรียน 2.4–2.6 หลักสอน widget ที่เป็นของชิ้นเดียว - ปุ่ม สวิตช์ สไลเดอร์ วางเรียงกันบนจอ
# ไฟล์นี้เปิดอีกกลุ่มหนึ่ง คือ widget ที่ทำหน้าที่เป็น "ที่ใส่ของ" ให้ widget อื่น
# ซึ่งเป็นทางเดียวที่จะเอาของหลายสิบชิ้นมาอยู่บนจอ 792x398 โดยที่คนยังอ่านออก
#
# ดูที่จอ: แท็บสามใบด้านบน แตะสลับได้
#         แท็บ 1 มีหน้าต่างที่มีแถบหัวเรื่องของตัวเอง
#         แท็บ 2 มีจอจุดที่ไฟวิ่ง และวงกลมหมุนบอกว่ากำลังทำงาน
#         แท็บ 3 มีไทล์ที่ปัดขึ้นลงได้
#
# กับดักที่ต้องรู้ ก่อนเอาไปใช้จริง:
#   - เมธอดของ "ที่ใส่ของ" คนละชื่อกับของที่ใส่รายการ
#     Tabview ใช้ .add_tab()   Tileview ใช้ .add_tile()
#     Win ใช้ .content()       Menu ใช้ .add_page()
#     ทั้งสี่คืน "แฮนเดิลของช่องข้างใน" ซึ่งต้องส่งเป็น parent= ตอนสร้างของที่จะวางเข้าไป
#     ลืมส่ง parent = ของไปโผล่บนจอหลัก ไม่ได้อยู่ในแท็บ และไม่มีอะไรฟ้อง
#   - ของที่อยู่ในแท็บที่ไม่ได้เปิดอยู่ ยังกินโควตาเท่าเดิม
#     แท็บช่วยเรื่องพื้นที่สายตา ไม่ได้ช่วยเรื่องโควตา
#   - ui.Spinner ไม่มีค่าให้อ่านและไม่ต้องสั่ง มันหมุนเอง
#     ใช้บอก "กำลังทำงาน" เท่านั้น ห้ามใช้บอกความคืบหน้า อันนั้นคือ ui.Bar
#
# รันจบเองใน 30 วินาที ไม่ต้องต่อเน็ต ไม่ต้องมีเซนเซอร์

import time
import ui

COL_TEXT, COL_DIM = 0xE8EAED, 0x9AA3AF
COL_CARD = 0x171B22

ui.screen()
time.sleep_ms(200)

# ---------- Tabview: value = ความสูงของแถบแท็บเป็นพิกเซล ----------
# สูง 336 ไม่ใช่ 398 - มุมขวาล่างเป็นปุ่มคอนโซลที่จองไว้ (690,340)
# Tabview เต็มจอจะทับมันเสมอ แล้วผู้เรียนกดเปิดคอนโซลไม่ได้ทั้งบทเรียน
tabs = ui.Tabview(x=0, y=0, w=792, h=336, value=44)

# .add_tab() คืนแฮนเดิลของ "หน้า" ในแท็บนั้น ไม่ใช่แฮนเดิลของตัวแท็บ
# ท่าที่ 1 - สร้างแท็บสามใบ
# คำใบ้: เมธอดชื่อ .add_tab("ชื่อแท็บ") และมันคืนแฮนเดิลของหน้าในแท็บนั้น
#        ต้องเก็บค่าที่คืนมาไว้ ไม่งั้นวางของเข้าไปข้างในไม่ได้
tab_win = ____
tab_dots = ____
tab_tile = ____

# ---------- แท็บ 1: Win ----------
# Win คือกล่องที่มีแถบหัวเรื่องติดมาด้วย ต่างจาก Panel ที่เป็นสี่เหลี่ยมเปล่า
# .content() คืนพื้นที่ใต้แถบหัว ถามซ้ำได้ค่าเดิม
win = ui.Win(text="บันทึกเหตุการณ์", x=16, y=12, w=740, h=280,
             value=40, parent=tab_win)
# ท่าที่ 2 - ขอพื้นที่เนื้อหาใต้แถบหัวเรื่อง
# คำใบ้: Win ใช้เมธอดชื่อ .content() ไม่ใช่ .add_tab()
body = ____
ui.Label("ระบบเริ่มทำงาน", x=16, y=16, color=COL_DIM, value=20, parent=body)
ui.Label("เชื่อมต่อเซนเซอร์แล้ว", x=16, y=48, color=COL_DIM, value=20, parent=body)
ui.Label("รอข้อมูลชุดแรก", x=16, y=80, color=COL_DIM, value=20, parent=body)

# ---------- แท็บ 2: DotMatrix + Spinner + Image ----------
# DotMatrix วาดจุดเป็นตาราง cols/rows คือจำนวนจุด ไม่ใช่พิกเซล
dots = ui.DotMatrix(x=24, y=24, w=176, h=176, color=0x4A9EFF,
                    cols=8, rows=8, parent=tab_dots)
# Spinner หมุนเอง ไม่ต้องสั่ง ไม่มีค่าให้อ่าน
ui.Spinner(x=252, y=56, w=80, h=80, parent=tab_dots)
ui.Label("กำลังอ่านเซนเซอร์", x=232, y=148, color=COL_DIM, value=20,
         parent=tab_dots)
# Image รับไอคอนที่คอมไพล์มากับเฟิร์มแวร์ ผ่าน .icon()
pic = ui.Image(x=420, y=44, w=96, h=96, parent=tab_dots)
pic.icon(0)
ui.Label("ui.Image + .icon(0)", x=396, y=152, color=COL_DIM, value=20,
         parent=tab_dots)

# ---------- แท็บ 3: Tileview ----------
# .add_tile(col, row) คืนแฮนเดิลของไทล์นั้น ปัดนิ้วเพื่อเลื่อนระหว่างไทล์
tiles = ui.Tileview(x=16, y=12, w=740, h=280, parent=tab_tile)
# ท่าที่ 3 - สร้างไทล์สองใบ เรียงในแนวตั้ง
# คำใบ้: .add_tile(col, row) - คอลัมน์เดียวกัน แถวต่างกัน จึงปัดขึ้นลงได้
t0 = ____
t1 = ____
ui.Label("ปัดขึ้นเพื่อดูหน้าถัดไป", x=24, y=112, color=COL_TEXT, value=24,
         parent=t0)
ui.Label("นี่คือไทล์ที่สอง", x=24, y=112, color=COL_TEXT, value=24, parent=t1)

# ---------- ลูปเหตุการณ์ ----------
# กฎข้อ 1 ของโมดูล ui: เรียก ui.poll() ทุกรอบ แม้จะไม่ได้ใช้ค่าที่ได้
taps = 0
for i in range(75):
    # ท่าที่ 4 - อ่านเหตุการณ์ทุกรอบ ตามกฎข้อ 1 ของโมดูล ui
    # คำใบ้: for _ev in ____:
    for _ev in ____:
        taps += 1
    # ไล่จุดทีละแถวให้เห็นว่า DotMatrix วาดได้จริง
    dots.set_pixels([(i + k) % 8 for k in range(8)])
    time.sleep_ms(400)

print("s04b: แตะทั้งหมด", taps, "ครั้ง")
print("Tabview .add_tab() · Win .content() · Tileview .add_tile()")
print("ทั้งสามคืนแฮนเดิลของช่องข้างใน ต้องส่งเป็น parent= เสมอ")
