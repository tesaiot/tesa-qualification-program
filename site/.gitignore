# Build output and dependencies
dist/
.astro/
node_modules/
# No lockfile is committed yet: CI runs `npm install` against the exact versions in package.json.
# Commit package-lock.json once it exists and switch the workflow to `npm ci`.

# Generated by scripts/sync_content.py (never edit by hand; the repo files are the source)
src/content/docs/
src/generated/
public/content-assets/
# Written by tools/export.py in CI
src/data/
# Rendered by scripts/render_slides.sh
public/slides/

npm-debug.log*
.DS_Store

# Python bytecode from running the scripts locally
__pycache__/
